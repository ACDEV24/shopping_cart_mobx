import 'package:flutter/material.dart';

import 'contants.dart';

class ScreenSize {

  static double height = MediaQuery.of(globalContext).size.height;
  static double width = MediaQuery.of(globalContext).size.width;
  
}